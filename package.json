{
  "name": "mal",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "clean": "turbo clean || rm -rf node_modules apps/*/node_modules"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "bun@1.2.10",
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.10",
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3"
  }
}
